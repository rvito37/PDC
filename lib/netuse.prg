* netuse.prg - STUB (placeholder until real file is uploaded)
* Network file/table open with retry

FUNCTION NetUse( cFile, lExclusive, nRetries )
   HB_SYMBOL_UNUSED( cFile )
   HB_SYMBOL_UNUSED( lExclusive )
   HB_SYMBOL_UNUSED( nRetries )
RETURN .T.
